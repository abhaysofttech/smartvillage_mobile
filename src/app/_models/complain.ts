export interface complainname {
    complainname: string;
    complainTitle: string;
}
export interface subcomplainname {
    complainname: string;
    subcomplainname: string;
    complainTitle: string;
}